{
  "name": "drillos-monorepo",
  "version": "1.0.0",
  "description": "Drillos - Flutter frontend + Node.js backend",
  "private": true,
  "scripts": {
    "build": "npm --prefix backend run build",
    "start": "cd backend && npx prisma db push && node dist/server.js",
    "install": "cd backend && npm ci --only=production"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
