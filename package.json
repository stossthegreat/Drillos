{
  "name": "drillos-monorepo",
  "version": "1.0.0",
  "description": "DrillOS - The active habit operating system",
  "private": true,
  "workspaces": [
    "backend"
  ],
  "scripts": {
    "build": "cd backend && npm run build",
    "start": "cd backend && npm start"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}