pluginManagement {
    includeBuild("${System.getenv("FLUTTER_ROOT") ?: "../../"}packages/flutter_tools/gradle")
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
include(":app")
