name: drillos
description: "Drill OS — The active habit operating system."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Core UI + Routing
  cupertino_icons: ^1.0.8
  go_router: ^13.2.5
  google_fonts: ^6.2.1
  http: ^1.2.2
  shared_preferences: ^2.2.3

  # Audio + Speech
  audioplayers: ^5.2.1

  # Notifications + Real Alarms
  flutter_local_notifications: ^19.4.2
  android_alarm_manager_plus: ^5.0.0
  timezone: ^0.10.1

  # Utils
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/audio/
    - assets/images/
    - assets/avatars/
